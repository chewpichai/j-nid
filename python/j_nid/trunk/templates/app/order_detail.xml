<?xml version="1.0" encoding="utf-8"?>
<response>
    <order>
        <id>{{ order.id }}</id>
        <person>{{ order.person.id }}</person>
        {% for order_item in order.order_items.all %}
        <order_item>
            <product>{{ order_item.product.id }}</product>
            <unit>{{ order.unit }}</unit>
            <price_per_unit>{{ order.price_per_unit }}</price_per_unit>
            <amount>{{ order.amount }}</amount>
        </order_item>
        {% endfor %}
    </order>
</response>