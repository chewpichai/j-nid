<person>
    <id>{{ person.id }}</id>
    <name>{{ person.name }}</name>
    <first_name>{{ person.first_name }}</first_name>
    <last_name>{{ person.last_name }}</last_name>
    <address>{{ person.address }}</address>
    <id_card_number>{{ person.id_card_number }}</id_card_number>
    <detail1>{{ person.detail1 }}</detail1>
    <detail2>{{ person.detail2 }}</detail2>
    {% for phone_number in person.phone_numbers.all %}
    <phone_number>
        <number>{{ phone_number.number }}</number>
        <type>{{ phone_number.type }}</type>
    </phone_number>
    {% endfor %}
    {% for bank_account in bank_accounts %}
    <bank_account>
        <number>{{ bank_account.number }}</number>
        <bank>{{ bank_account.bank }}</bank>
    </bank_account>
    {% endfor %}
</person>