<?xml version="1.0" encoding="utf-8"?>
<product_stats name="{{ name }}">
{% for stat in stats %}
    <stat quantity="{{ stat.quantity }}" price_per_unit="{{ stat.price_per_unit }}" />
{% endfor %}
</product_stats>